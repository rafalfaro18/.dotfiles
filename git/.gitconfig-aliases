[alias]
    co = checkout
    ci = commit
    st = status
    br = branch
    sm = submodule
    # initialize and update all nested submodules at once
    sup = !git submodule update --init --recursive
    # git sfe 'git pull' to run a command across every submodule
    sfe = submodule foreach
    # quickly add a new submodule
    sadd = !git submodule add

[core]
excludesfile = ~/.gitignore_global

[color]
    ui = true
[color "diff"]
    meta = 111
    frag = 141
    old = 204
    new = 120

# Add this to ~/.gitconfig and uncomment to isolate github credentials per repo
#[credential "https://github.com"]
#    useHttpPath = true

# Add this to ~/.gitconfig and uncomment to import this file
# [include]
#    path = ~/.gitconfig-aliases
